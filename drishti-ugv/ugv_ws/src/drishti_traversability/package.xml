<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>drishti_traversability</name>
  <version>0.1.0</version>
  <description>
    Camera-derived terrain cost. Fuses elevation geometry (slope, roughness,
    height variance, step) with semantic class and observation uncertainty into
    a single traversability field, and projects it into a Nav2 costmap layer.

    This is the component specific to the problem statement: Nav2, RTAB-Map and
    the elevation mapper are reused, but turning vision into terrain judgement
    a planner will respect is the actual work. SPEC.md section 6.
  </description>

  <!-- TODO: replace with a real team contact before the repository is shared. -->
  <maintainer email="team@thevikings.example">The Vikings</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>pluginlib</depend>
  <depend>nav2_costmap_2d</depend>
  <depend>grid_map_core</depend>
  <depend>grid_map_ros</depend>
  <depend>grid_map_msgs</depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <nav2_costmap_2d plugin="${prefix}/plugins.xml"/>
  </export>
</package>
